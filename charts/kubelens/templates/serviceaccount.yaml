{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "kubelens.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "kubelens.labels" . | nindent 4 }}
{{- end }}
